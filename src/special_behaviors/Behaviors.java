package special_behaviors;

/**
 * Enum to represent the different special behaviors that can be added to the game.
 */
public enum Behaviors {
    EXTRA_PUCK, EXTRA_PADDLE, CAMERA_CHANGE, EXTRA_LIFE, DOUBLE_BEHAVIOR, NONE;
}
